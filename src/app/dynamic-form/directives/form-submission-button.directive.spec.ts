import { FormSubmissionButtonDirective } from './form-submission-button.directive';

describe('FormSubmissionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FormSubmissionButtonDirective();
    expect(directive).toBeTruthy();
  });
});
