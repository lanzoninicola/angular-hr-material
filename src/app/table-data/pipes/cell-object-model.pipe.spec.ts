import { CellObjectModelPipe } from './cell-object-model.pipe';

describe('CellObjectModelPipe', () => {
  it('create an instance', () => {
    const pipe = new CellObjectModelPipe();
    expect(pipe).toBeTruthy();
  });
});
