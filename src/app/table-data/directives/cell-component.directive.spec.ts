import { CellComponentDirective } from './cell-component.directive';

describe('CellComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new CellComponentDirective();
    expect(directive).toBeTruthy();
  });
});
