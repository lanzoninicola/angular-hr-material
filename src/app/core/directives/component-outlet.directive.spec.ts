import { ComponentOutletDirective } from './component-outlet.directive';

describe('ComponentOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentOutletDirective();
    expect(directive).toBeTruthy();
  });
});
